name: petstore
description: 'A sample pet store API with users, pets, and orders'
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  dio: ^5.7.0
  collection: ^1.19.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
